# Configuration Serveur
PORT=3000
NODE_ENV=production

# Base de données PostgreSQL
# Railway fournit automatiquement DATABASE_URL quand vous ajoutez PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/database

# Frontend URL (optionnel, pour CORS)
FRONTEND_URL=https://votre-frontend.vercel.app

# Note: Sur Railway, ces variables sont automatiquement configurées
# Vous devez juste ajouter un service PostgreSQL à votre projet
